const PrivateChat = require("../models/privatechat_model");

module.exports = function(server){
// const io = require('socket.io')(server);
// const private = io.of('/privatechat');
//
//       private.on('connection', (socket) => {
//         console.log(socket)
//    });
}
